<!DOCTYPE html>
<html lang="en">
<head>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
      body { font-family: 'Inter', sans-serif; }
    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-900 min-h-screen">
    <div class="w-full min-h-screen bg-gray-50 text-gray-900 p-4 sm:p-10 flex flex-col justify-center items-center">
        <div class="w-full max-w-2xl flex justify-end mb-4">
            <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block">logout</a>
        </div>
        <h5 class="mb-5 text-lg sm:text-xl">Edit your post</h5>
        <form action="/update/<%= post._id %>" method="post" class="w-full max-w-2xl flex flex-col gap-3 bg-white border border-gray-200 rounded-xl shadow-sm p-4">
            <textarea placeholder="What's on your mind?" class="p-3 outline-none w-full bg-white border-2 border-gray-300 text-gray-900 rounded-md resize-none min-h-[100px]" name="content"></textarea>
            <input class="px-3 w-full sm:w-40 py-2 text-sm bg-indigo-500 text-white rounded-md mt-2 cursor-pointer hover:bg-indigo-600" type="submit" value="Update Post">
        </form>
    </div>
</body>
</html>
